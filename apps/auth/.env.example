# Set these as Cloudflare Worker secrets via `wrangler secret put <NAME>`
BETTER_AUTH_SECRET=your-secret-here
BETTER_AUTH_URL=https://auth.asu.app
JWT_SECRET=shared-secret-with-sync-worker
CORS_ORIGINS=https://asu.app
