# Implementation Plans

Generated by the improve skill on 2026-09-18 (audit at commit `ae62019`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (vitest, worker typechecks, CI) | P1 | M | — | TODO |
| 002 | Fix "Mit Trupp starten" squad creation | P1 | S | — (test step wants 001) | TODO |
| 003 | Stop logging OTPs + upgrade better-auth ≥ 1.6.22 | P1 | M | 001 | TODO |
| 004 | Enforce org membership on all R2 file keys | P1 | M | — | TODO |
| 005 | Sync-token refresh + honest org error states | P1 | M | — (001 recommended) | TODO |
| 006 | Pause accounting + archived-squad history | P1 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first.** 003 needs the worker typecheck scripts and the auth smoke checklist; 006 needs the materializer test harness (it changes an event schema — the legacy-event regression test is the safety net). 002 and 005 can run before 001 but their regression-test steps degrade to manual verification.
- 004 and 005 are independent of everything else and of each other.
- 002, 006 both touch squad UI components but disjoint files; safe in parallel.
- 004 leaves a **residual risk** until the operator migrates legacy `operations/*` R2 objects: those objects stay readable (write-protected) by any authenticated user. Tracked in plan 004, Step 3.
- Operator-only follow-ups from 003: provision an email provider key (`wrangler secret put RESEND_API_KEY`), treat historical Cloudflare logs containing OTPs as burned.

## Audited but not planned (findings that remain open)

From the 2026-09-18 audit, in leverage order — candidates for the next planning round:

- CSV/FeuerOn import correctness (import reports success without importing; date-cell handling) — finding #9.
- Invite hardening: ~32-bit codes, no rate limit, non-atomic redeem — finding #10.
- Docs/DX: CLAUDE.md describes the wrong stack (says Postgres/Hetzner + tsx; reality D1 + wrangler), template README, missing `.env.example` for web/sync — finding #11.
- Perf: `@react-pdf/renderer` in the route chunk (~1.6 MB), 1 Hz CurrentTime context fan-out, duplicate per-card LiveStore subscriptions, event-log collector architecture — findings #12, #15, #18.
- Worker hardening bundle: `Content-Length`-trusting size check, raw error strings to clients, no upload content-type allowlist, unpinned JWT alg/claims, fail-open `storeId` check, `ADMIN_SECRET` must be set — finding #14.
- Slug collisions + missing-slug crash on Einsatz routes — finding #13.
- Misc small fixes (org bootstrap cancellation — partially covered by 005, invite-redeem redirect, blob-URL revocation, RecordKeeper stale state) and dead-code cleanup (`record-keeper.tsx`, `complete-operation.tsx`, duplicate `cn()`, `s3-cors.json`, `s3.ts` naming) — findings #16, #17.
- Direction options (maintainer's call): Export tab (currently a stub route), admin invite UI (API exists, no frontend), PWA/offline installability, personnel export to match import.

## Findings considered and rejected

- `SameSite=None` session cookies flagged as CSRF surface: **by design** — auth worker and SPA live on different origins; revisit only if hosting consolidates to one site.
- Chasing all 99 `pnpm audit` advisories: only better-auth (plan 003) and hono (low, patch at leisure) touch reachable runtime code; the rest are dev-time (vite/postcss) or transitive in LiveStore internals.
- LiveStore 0.3.x / `wa-sqlite 1.0.5-dev.2` upgrade: not now — pre-1.0 event-sourcing core with no test net; revisit after 001 has been green for a while.
- Event sourcing full-replay cost: documented LiveStore architecture (`apps/web/livestore-migrations.md`), not a finding.
- In-repo `livestore-manual/`: kept — it is actively useful to executors (001 and 005 cite it); maintainer preference.
